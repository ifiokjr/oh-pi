# @monopi/extension-bg-process

Compatibility pi extension package for the legacy `bg-process` extension id. Prefer `@monopi/background-tasks` for new installs.

```bash
pi install npm:@monopi/extension-bg-process
```
