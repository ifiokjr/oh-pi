---
monopi: patch
---

Diagnostics prompt completion summaries now render through UI notifications instead of custom chat messages so they remain visible without adding hidden turns or prompt context for the LLM.
