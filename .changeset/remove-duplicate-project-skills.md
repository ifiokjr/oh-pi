---
monopi: patch
---

# Remove duplicate project skills

## Fixed

- Removed the project-provided `devenv` and `git-workflow` skills from `@ifi/oh-pi-skills` so they no longer collide with user-level skills.
- Updated skill package docs and feature catalog counts to match the remaining skill set.
