---
default: patch
---

# Revert to space-separated slash subcommands

Revert extension command aliases, tests, and documentation from colon-separated slash subcommands back to space-separated subcommands, such as `/skill flutter` and `/providers login`.
