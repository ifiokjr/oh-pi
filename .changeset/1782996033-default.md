---
monopi: patch
---

# Format TOML and Markdown project files

Project formatting now uses a single lint:fix command that runs oxfmt before oxlint, and monochange.toml is kept in oxfmt-compatible TOML formatting.
