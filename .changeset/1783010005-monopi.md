---
monopi: patch
---

# Rename lockstep group to monopi

The MonoChange lockstep group is now named `monopi` and contains every configured package; changeset validation and agent release guidance now target monopi frontmatter.
